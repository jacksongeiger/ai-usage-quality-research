# ASSUMPTIONS LOG

Every assumption made because the human was asleep and could not be asked. Each is the most reasonable call given the brief; flagged so they can be overturned on review.

| # | Assumption | Why reasonable | Impact if wrong |
|---|---|---|---|
| A1 | The org has "team admin" / metrics-API access to whatever AI tools it pays for (Copilot, Cursor, Claude Code, etc.), even if not all of that telemetry is in Snowflake yet. | The brief says token usage *is* in Snowflake; ingesting the richer vendor telemetry is a config/ETL step, not a capability gap. | Some "High value, turn-it-on" data recs become harder than rated. Flagged per-source in deliverable 04. |
| A2 | "Token usage per engineer per tool" can be time-bucketed (daily/weekly) and is attributable to a known individual identity that joins to GitHub identity. | Standard for any usage table feeding a per-engineer dashboard; identity-join is the universal precondition and is called out as such. | Without identity-join the whole per-engineer framing fails; treated as precondition P0 in deliverable 01. |
| A3 | "Review outcomes (approved / changes requested / commented)" are available per-PR with timestamps and reviewer/author identity (metadata, not comment bodies). | The brief lists these explicitly as available CI/review metadata. | Review-friction metrics degrade to coarser forms; noted in metric failure modes. |
| A4 | LOC metadata includes added/deleted line counts per commit/PR and commit timestamps — enough to compute churn/line-survival GitClear-style — but NOT diff contents. | GitClear proves churn/survival are computable from diff *metadata* alone; the brief grants LOC + commit + timestamp metadata. | Churn/survival metrics weaken to revert-detection only; flagged. |
| A5 | The deliverable audience is the engineer plus possibly a coach/enablement function acting on the engineer's behalf — never managers for evaluation. | The brief is explicit that output goes to the engineer for self-improvement. | If managers see it, the entire ethics design (deliverable 06) must harden further; already argued as an architectural guarantee. |
| A6 | "Quality of AI usage" means *the engineer is getting durable, low-friction leverage from AI* — not "the engineer's code is good" and not "the engineer is productive/valuable." | Matches the brief's illustrative destination and the no-code constraint; keeps construct honest. | Construct drift toward code-quality or performance scoring; guarded against repeatedly. |
| A7 | A multi-week observation window (≥4 weeks) is acceptable before generating recommendations. | Individual weekly samples are tiny/noisy (validity research); recs are for self-improvement, not real-time. | Faster cadence would need wider error bars; noted in normalization section. |
| A8 | The org runs CI such that "first-pass build success" is meaningful (PRs trigger builds; reruns are detectable). | Standard for any org with the CI build-state data the brief lists. | First-pass CI metric weakens; flagged as data-dependent. |
| A9 | Engineers use, on average, more than one AI tool (DX research: ~82% use 3+). So per-tool signals must be normalized/aggregated across each person's actual tool set. | Strongly supported by vendor research; avoids undercounting. | If single-tool shops, normalization is simpler, not wrong. |
| A10 | This is an analysis artifact, not a system to be built now; therefore no code, schemas, or pipelines are produced — only the framework and logic. | The contract explicitly forbids drifting into product/build. | N/A — re-checked every cycle. |
